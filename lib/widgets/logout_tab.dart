import 'package:flutter/material.dart';

class LogoutTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Logout Tab')),
    );
  }
}
